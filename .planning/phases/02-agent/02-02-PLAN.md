---
phase: 02-agent
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/main/ai/agents/casting-director/index.ts
autonomous: true
requirements:
  - CAST-03
must_haves:
  truths:
    - "选角导演 Agent 实现目录已创建"
    - "runCastingDirectorAgent 主函数已导出"
    - "类型定义 CastingDirectorResult、CastingDirectorContext、CastingDirectorAgentOptions 已导出"
    - "提示词构建函数 buildSystemPrompt、buildUserPrompt 已实现"
    - "输出解析函数 parseOutput 已实现"
  artifacts:
    - path: "src/main/ai/agents/casting-director/index.ts"
      provides: "Agent 完整实现"
      exports:
        - "runCastingDirectorAgent"
        - "CastingDirectorResult"
        - "CastingDirectorContext"
        - "CastingDirectorAgentOptions"
      contains:
        - "function buildSystemPrompt"
        - "function buildUserPrompt"
        - "function parseOutput"
  key_links:
    - from: "buildSystemPrompt"
      to: "CASTING_DIRECTOR_AGENT_EDITABLE_PART + CASTING_DIRECTOR_AGENT_LOCKED_PART"
      via: "模板常量导入和字符串合并"
    - from: "runCastingDirectorAgent"
      to: "getGlobalProvider().generateText()"
      via: "provider-manager 调用 LLM"
    - from: "parseOutput"
      to: "CastingDirectorResult"
      via: "JSON 解析和字段验证"
---

<objective>
创建选角导演 Agent 的实现目录和主文件，实现完整的 Agent 调用逻辑。

Purpose: 将选角导演从「透传模式」改造为「AI 生成模式」，调用 LLM 为每个角色生成三视图图像提示词。
Output: `src/main/ai/agents/casting-director/index.ts` 文件，包含类型定义、提示词构建、输出解析和主函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent/02-CONTEXT.md
@.planning/phases/02-agent/02-RESEARCH.md

## 参考实现（必须遵循的模式）

### Agent 实现结构参考
- `src/main/ai/agents/art-director/index.ts` — Agent 实现标准结构
- `src/main/ai/agents/screenplay/index.ts` — 单次调用型 Agent 参考

### 关键模式
1. **类型定义区**: CastingDirectorResult, CastingDirectorContext, CastingDirectorAgentOptions
2. **提示词构建区**: buildSystemPrompt(), buildUserPrompt()
3. **输出解析区**: parseOutput()
4. **Agent 主函数**: runCastingDirectorAgent()

### 用户决策（来自 CONTEXT.md）
- **D-04**: 选角导演核心职责：接收艺术总监角色描述，生成三视图图像提示词，确保角色形象一致
- **D-06**: 采用标准目录结构 `src/main/ai/agents/casting-director/index.ts`
- **D-08**: 输出 JSON 结构保持向后兼容，新增 `image_generation_prompts` 字段

### 输出 JSON 结构
```typescript
{
  character_profiles: [
    {
      id: string;
      name: string;
      role_type: 'protagonist' | 'antagonist' | 'supporting';
      appearance: string;
      costume: string;
      personality_traits: string[];
      key_actions: string[];
      image_generation_prompts: {
        front_view: string;
        side_view: string;
        action_pose: string;
      };
    }
  ],
  scene_breakdowns: SceneBreakdown[];
}
```

### 输入上下文结构
```typescript
interface CastingDirectorContext {
  artDirectorOutput: ArtDirectorResult; // 艺术总监输出
  aspectRatio: string; // 画幅比例
}
```

### 当前缺陷（必须修复）
- `src/main/ai/workflows/nodes/casting-director.ts` 第 36-49 行直接透传 artDirectorOutput，不调用 AI
- 必须改为调用 LLM 生成图像提示词
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 casting-director/index.ts Agent 实现</name>
  <read_first>
    - src/main/ai/agents/art-director/index.ts
    - src/main/ai/agents/screenplay/index.ts
    - src/shared/constants/castingDirectorTemplates.ts
  </read_first>
  <files>src/main/ai/agents/casting-director/index.ts</files>
  <action>
创建 `src/main/ai/agents/casting-director/index.ts` 文件，包含以下内容：

1. **文件头注释**：说明 Agent 职责和调用流程

2. **导入语句**：
   - `import type { ArtDirectorResult } from '@shared/types/aside';` — 艺术总监输出类型
   - `import { CASTING_DIRECTOR_AGENT_EDITABLE_PART, CASTING_DIRECTOR_AGENT_LOCKED_PART, CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE } from '@shared/constants/castingDirectorTemplates';`
   - `import { getGlobalProvider } from '../../provider-manager';`

3. **类型定义区**：
   - `CastingDirectorCharacterProfile` 接口：包含艺术总监的所有字段 + `image_generation_prompts` 对象
   - `CastingDirectorResult` 接口：包含 `character_profiles: CastingDirectorCharacterProfile[]` 和 `scene_breakdowns`
   - `CastingDirectorContext` 接口：包含 `artDirectorOutput: ArtDirectorResult` 和 `aspectRatio: string`
   - `CastingDirectorAgentOptions` 接口：包含 `modelId?: string`, `customEditablePart?: string`

4. **提示词构建区**：
   - `buildSystemPrompt(customEditablePart?: string): string` — 合并 EDITABLE + LOCKED
   - `buildUserPrompt(context: CastingDirectorContext, logger?): string` — 注入变量：
     * 使用 `JSON.stringify(context.artDirectorOutput.character_profiles, null, 2)` 填充 {{characterProfiles}}
     * 使用 `context.artDirectorOutput.script_brief.visual_style_tags.join(', ')` 填充 {{visualStyleTags}}
     * 使用 `context.aspectRatio` 填充 {{aspectRatio}}
   - 日志输出（如果 logger 存在）：记录 systemPromptLength, userPromptLength, characterCount

5. **输出解析区**：
   - `parseOutput(llmOutput: string): CastingDirectorResult` — 提取 JSON 代码块并解析
   - 验证必要字段：character_profiles 必须存在且为数组
   - 透传 scene_breakdowns（从 artDirectorOutput 获取）

6. **Agent 主函数**：
   - `runCastingDirectorAgent(context, options, logger?): Promise<CastingDirectorResult>`
   - 步骤：
     1. 获取 provider（getGlobalProvider()）
     2. 构建提示词
     3. 调用 LLM：`provider.generateText(userPrompt, { systemPrompt, temperature: 0.7, maxTokens: 4096 })`
     4. 解析输出并返回

7. **默认导出**：`export default runCastingDirectorAgent;`

代码风格必须与 art-director/index.ts 完全一致，包括：
- 使用 `// ─── 类型定义 ─────────────────────────────────────────────` 分隔区
- 使用相同的日志输出格式
- 使用相同的错误处理方式
  </action>
  <verify>
    <automated>grep -n "runCastingDirectorAgent\|CastingDirectorResult\|CastingDirectorContext\|CastingDirectorAgentOptions\|buildSystemPrompt\|buildUserPrompt\|parseOutput" src/main/ai/agents/casting-director/index.ts | head -20</automated>
  </verify>
  <done>
    - 文件 src/main/ai/agents/casting-director/index.ts 已创建
    - 导出 runCastingDirectorAgent 主函数
    - 导出 CastingDirectorResult、CastingDirectorContext、CastingDirectorAgentOptions 类型
    - 实现 buildSystemPrompt、buildUserPrompt、parseOutput 函数
    - 代码风格与 art-director/index.ts 一致
  </done>
</task>

</tasks>

<verification>
1. 文件结构检查：确认文件包含类型定义、提示词构建、输出解析、主函数四个区域
2. 导出检查：确认主函数和类型都已导出
3. 导入检查：确认正确导入 castingDirectorTemplates 常量
4. 模式一致性：确认代码风格与现有 Agent 实现一致
</verification>

<success_criteria>
- src/main/ai/agents/casting-director/index.ts 文件存在且可编译
- 文件导出 runCastingDirectorAgent、CastingDirectorResult、CastingDirectorContext、CastingDirectorAgentOptions
- buildSystemPrompt 正确合并 EDITABLE + LOCKED
- buildUserPrompt 正确注入 {{characterProfiles}}、{{visualStyleTags}}、{{aspectRatio}} 变量
- parseOutput 正确解析 JSON 并返回 CastingDirectorResult
- 主函数正确调用 provider.generateText() 并返回结果
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent/02-02-SUMMARY.md`
</output>
