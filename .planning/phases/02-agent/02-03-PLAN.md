---
phase: 02-agent
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/shared/constants/promptTemplates.ts
  - src/main/ai/workflows/nodes/casting-director.ts
autonomous: true
requirements:
  - CAST-04
  - CAST-05
must_haves:
  truths:
    - "选角导演 Agent 已在 BUILTIN_PROMPT_TEMPLATES 中注册"
    - "LangGraph Node 已重构调用 runCastingDirectorAgent"
    - "导演模式 humanApproval 逻辑已添加"
    - "PromptStudio 能正确展示选角导演 Agent"
  artifacts:
    - path: "src/shared/constants/promptTemplates.ts"
      provides: "BUILTIN_PROMPT_TEMPLATES 注册"
      contains:
        - "agentId: 'casting-director-agent'"
        - "import { CASTING_DIRECTOR_AGENT_EDITABLE_PART, CASTING_DIRECTOR_AGENT_LOCKED_PART, CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE, CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE }"
    - path: "src/main/ai/workflows/nodes/casting-director.ts"
      provides: "LangGraph Node 重构"
      contains:
        - "import { runCastingDirectorAgent } from '../../agents/casting-director'"
        - "runCastingDirectorAgent(...)"
        - "if (state.executionMode === 'director') { updates.humanApproval = false; }"
  key_links:
    - from: "casting-director.ts Node"
      to: "runCastingDirectorAgent"
      via: "import 和函数调用"
    - from: "BUILTIN_PROMPT_TEMPLATES"
      to: "CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE"
      via: "数组添加新条目"
    - from: "导演模式判断"
      to: "humanApproval = false"
      via: "state.executionMode === 'director'"
---

<objective>
在 BUILTIN_PROMPT_TEMPLATES 中注册选角导演 Agent，并重构 LangGraph Node 以调用新的 Agent 实现。

Purpose: 完成选角导演 Agent 的集成，使其在 PromptStudio 中可展示，并在工作流中正确调用 AI 生成图像提示词。
Output: 修改后的 `promptTemplates.ts` 和 `casting-director.ts` 文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent/02-CONTEXT.md
@.planning/phases/02-agent/02-RESEARCH.md

## 参考实现（必须遵循的模式）

### BUILTIN_PROMPT_TEMPLATES 注册参考
- `src/shared/constants/promptTemplates.ts` 第 200-212 行 — 艺术总监 Agent 注册示例

### 导演模式 humanApproval 参考
- `src/main/ai/workflows/nodes/art-director.ts` 第 75-77 行
```typescript
if (state.executionMode === 'director') {
  updates.humanApproval = false;
}
```

### 当前 Node 实现（待重构）
- `src/main/ai/workflows/nodes/casting-director.ts` 第 36-49 行直接透传 artDirectorOutput，不调用 AI
- 必须改为调用 runCastingDirectorAgent

### 用户决策（来自 CONTEXT.md）
- **D-05**: 导演模式 `executionMode === 'director'` 时设置 `humanApproval = false`，快速模式不设此字段
- **D-07**: LangGraph Node 保持向后兼容，内部调用新的 `runCastingDirectorAgent()` 函数
- **D-09**: 工作流只生成提示词，不直接调用图像生成 API

### Node 输入/输出
输入：`WorkflowState`，包含 `step2_characters`（艺术总监输出）
输出：`Partial<WorkflowState>`，包含 `step3_storyboard` 和 `currentStep: 4`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 BUILTIN_PROMPT_TEMPLATES 中注册选角导演 Agent</name>
  <read_first>
    - src/shared/constants/promptTemplates.ts
    - src/shared/constants/castingDirectorTemplates.ts
  </read_first>
  <files>src/shared/constants/promptTemplates.ts</files>
  <action>
修改 `src/shared/constants/promptTemplates.ts` 文件：

1. **添加导入语句**（在艺术总监导入之后）：
```typescript
// 导入选角导演 Agent 提示词
import {
  CASTING_DIRECTOR_AGENT_EDITABLE_PART,
  CASTING_DIRECTOR_AGENT_LOCKED_PART,
  CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE,
  CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE,
} from './castingDirectorTemplates';
```

2. **在 BUILTIN_PROMPT_TEMPLATES 数组中添加选角导演条目**（在艺术总监条目之后）：
```typescript
{
  agentId: 'casting-director-agent',
  agentName: '选角导演 Agent',
  agentDescription: '根据艺术总监的角色描述，生成三视图图像提示词（正面、侧面、动作姿态）',
  templateId: 'builtin-casting-director-v1',
  name: '内置默认模板 v1',
  editablePart: CASTING_DIRECTOR_AGENT_EDITABLE_PART,
  lockedPart: CASTING_DIRECTOR_AGENT_LOCKED_PART,
  userPromptTemplate: CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE,
  get systemPrompt() {
    return `${this.editablePart}\n\n${this.lockedPart}`;
  },
},
```

3. 确保代码风格与现有条目完全一致（缩进、引号、换行等）
  </action>
  <verify>
    <automated>grep -n "casting-director-agent\|CASTING_DIRECTOR_AGENT" src/shared/constants/promptTemplates.ts</automated>
  </verify>
  <done>
    - src/shared/constants/promptTemplates.ts 已修改
    - 已导入 CASTING_DIRECTOR_AGENT_EDITABLE_PART、CASTING_DIRECTOR_AGENT_LOCKED_PART、CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE、CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE
    - BUILTIN_PROMPT_TEMPLATES 数组已添加选角导演 Agent 条目
    - agentId 为 'casting-director-agent'，agentName 为 '选角导演 Agent'
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 LangGraph Node 调用 runCastingDirectorAgent</name>
  <read_first>
    - src/main/ai/workflows/nodes/casting-director.ts
    - src/main/ai/workflows/nodes/art-director.ts
    - src/main/ai/agents/casting-director/index.ts
  </read_first>
  <files>src/main/ai/workflows/nodes/casting-director.ts</files>
  <action>
修改 `src/main/ai/workflows/nodes/casting-director.ts` 文件：

1. **添加导入语句**：
```typescript
import { runCastingDirectorAgent, CastingDirectorResult } from '../../agents/casting-director';
```

2. **修改节点函数实现**（替换第 36-49 行的透传逻辑）：
```typescript
// 2. 调用选角导演 Agent 生成图像提示词
console.log('[Agent 3: 选角导演] 调用 AI 生成图像提示词');

const castingResult = await runCastingDirectorAgent(
  {
    artDirectorOutput: artDirectorOutput,
    aspectRatio: state.aspectRatio || '9:16',
  },
  {
    modelId: state.agentModelAssignments?.['casting-director-agent'],
  },
  {
    info: (msg: string, meta?: any) => console.log(msg, meta),
  }
);

const output: StepOutput<CastingDirectorResult> = {
  content: castingResult,
  metadata: {
    timestamp: Date.now(),
    duration: Date.now() - startTime,
    model: state.agentModelAssignments?.['casting-director-agent'] || 'default',
    tokens: 0, // TODO: 从 provider 返回结果中获取
  },
};
```

3. **添加导演模式 humanApproval 逻辑**（在返回 updates 之前）：
```typescript
// 3. 返回状态更新
const updates: Partial<WorkflowState> = {
  step3_storyboard: output,
  currentStep: 4,
};

// 导演模式：设置 humanApproval 等待用户确认
if (state.executionMode === 'director') {
  updates.humanApproval = false;
}

const endTime = Date.now();
console.log(`[Agent 3: 选角导演] 完成，耗时 ${endTime - startTime}ms`);

return updates;
```

4. **更新文件头注释**：
将原来的注释更新为：
```typescript
/**
 * 选角导演 Agent 节点
 * Agent 3: 为每个角色生成三视图图像提示词（正面、侧面、动作姿态）
 *
 * 注意：工作流只生成图像提示词，不直接调用图像生成 API
 * 图像实际生成由前端通过 aside:generate-character-image 处理
 */
```

5. **移除不再使用的导入**（如果 CastingDirectorAgentPrompts 不再使用）：
```typescript
// 删除这一行
import { CastingDirectorAgentPrompts } from '../../prompts/casting-director-agent';
```

6. 确保错误处理保持不变（try-catch 块）
  </action>
  <verify>
    <automated>grep -n "runCastingDirectorAgent\|humanApproval = false" src/main/ai/workflows/nodes/casting-director.ts</automated>
  </verify>
  <done>
    - src/main/ai/workflows/nodes/casting-director.ts 已修改
    - 已导入 runCastingDirectorAgent
    - 已调用 runCastingDirectorAgent 替代透传逻辑
    - 已添加导演模式 humanApproval = false 逻辑
    - 输出类型为 CastingDirectorResult
  </done>
</task>

</tasks>

<verification>
1. BUILTIN_PROMPT_TEMPLATES 检查：确认选角导演 Agent 已添加到数组中
2. Node 重构检查：确认已调用 runCastingDirectorAgent 而非透传
3. 导演模式检查：确认已添加 humanApproval 逻辑
4. 导入检查：确认所有导入路径正确
</verification>

<success_criteria>
- src/shared/constants/promptTemplates.ts 中 BUILTIN_PROMPT_TEMPLATES 包含 casting-director-agent 条目
- src/main/ai/workflows/nodes/casting-director.ts 导入并调用 runCastingDirectorAgent
- casting-director.ts Node 在导演模式下设置 humanApproval = false
- 代码可以编译通过（TypeScript 类型检查通过）
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent/02-03-SUMMARY.md`
</output>
