---
phase: 02-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/constants/castingDirectorTemplates.ts
autonomous: true
requirements:
  - CAST-01
  - CAST-02
must_haves:
  truths:
    - "选角导演 Agent 提示词已拆分为 EDITABLE、LOCKED、DYNAMIC 三层"
    - "提示词常量文件可被 main 和 renderer 进程同时引用"
    - "CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE 元数据对象已导出"
  artifacts:
    - path: "src/shared/constants/castingDirectorTemplates.ts"
      provides: "三层提示词定义和元数据导出"
      exports:
        - "CASTING_DIRECTOR_AGENT_EDITABLE_PART"
        - "CASTING_DIRECTOR_AGENT_LOCKED_PART"
        - "CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE"
        - "CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE"
  key_links:
    - from: "CASTING_DIRECTOR_AGENT_EDITABLE_PART"
      to: "CASTING_DIRECTOR_AGENT_LOCKED_PART"
      via: "CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE.systemPrompt getter"
---

<objective>
创建选角导演 Agent 的三层提示词常量文件，遵循与艺术总监 Agent 相同的架构模式。

Purpose: 将选角导演的提示词系统改造为可在 PromptStudio 中自定义的三层架构（EDITABLE + LOCKED + DYNAMIC）。
Output: `src/shared/constants/castingDirectorTemplates.ts` 文件，包含完整的提示词定义和元数据导出。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent/02-CONTEXT.md
@.planning/phases/02-agent/02-RESEARCH.md

## 参考实现（必须遵循的模式）

### 文件结构参考
- `src/shared/constants/artDirectorTemplates.ts` — 三层架构模板定义的标准模式
- `src/shared/constants/screenplayAgentTemplates.ts` — 另一个三层架构参考

### 关键模式
1. **EDITABLE 层**: Agent 人设、创作方法论、风格指南 —— 面向游戏行业专家，可在 PromptStudio 中自定义
2. **LOCKED 层**: JSON 输出格式、字段定义、自检清单 —— 代码解析依赖，不可修改
3. **DYNAMIC 层**: 运行时注入的上下文变量（角色描述、视觉风格标签、画幅比例）

### 用户决策（来自 CONTEXT.md）
- **D-01**: EDITABLE 层包含 Agent 人设、三视图创作方法论、图像提示词编写技巧、角色一致性保证方法
- **D-02**: LOCKED 层包含 JSON 输出格式、图像提示词字段约束、三视图结构定义、自检清单
- **D-03**: DYNAMIC 层包含角色描述、视频参数、视觉风格标签
- **D-08**: 输出 JSON 结构保持向后兼容，新增 `image_generation_prompts` 字段

### 变量清单
```
{{characterProfiles}} - 角色描述数组（JSON 字符串）
{{visualStyleTags}} - 视觉风格标签
{{aspectRatio}} - 画幅比例
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 castingDirectorTemplates.ts 三层提示词常量文件</name>
  <read_first>
    - src/shared/constants/artDirectorTemplates.ts
    - src/shared/constants/screenplayAgentTemplates.ts
  </read_first>
  <files>src/shared/constants/castingDirectorTemplates.ts</files>
  <action>
创建 `src/shared/constants/castingDirectorTemplates.ts` 文件，包含以下内容：

1. **文件头注释**：说明文件用途（同时被 main 和 renderer 进程引用）和分层设计

2. **EDITABLE 层（CASTING_DIRECTOR_AGENT_EDITABLE_PART）**：
   - Agent 人设定义：选角导演是专注于将角色描述转化为可用于图像生成的精确提示词的专业 Agent
   - 三视图创作方法论：
     * 正面视图：展示角色完整外貌、服装细节
     * 侧面视图：展示角色轮廓、体型比例
     * 动作姿态：展示角色性格、典型姿态
   - 图像提示词编写技巧：
     * 使用英文关键词（图像生成模型更理解英文）
     * 按重要性排序（主体 > 服装 > 风格 > 质量词）
     * 添加质量关键词（high quality, detailed, sharp focus, professional photography）
   - 角色一致性保证方法：确保三视图中角色的服装、发型、配饰保持一致
   - 风格一致性规则：所有角色必须使用相同艺术风格

3. **LOCKED 层（CASTING_DIRECTOR_AGENT_LOCKED_PART）**：
   - JSON 输出格式定义（严格遵守）
   - 字段约束：
     * `character_profiles` 数组，每个角色包含 id, name, role_type, appearance, costume, personality_traits, key_actions
     * 新增 `image_generation_prompts` 对象，包含 front_view, side_view, action_pose 三个英文提示词
     * `scene_breakdowns` 透传艺术总监的场景
   - 图像提示词约束：
     * 必须使用英文
     * 控制在 200 字以内
     * 包含质量关键词
   - 自检清单

4. **DYNAMIC 层（CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE）**：
   - 使用变量占位符：{{characterProfiles}}, {{visualStyleTags}}, {{aspectRatio}}
   - 模板结构包含角色描述、视觉风格、画幅比例三个部分
   - 明确要求为每个角色生成三视图图像提示词

5. **元数据导出（CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE）**：
   - agentId: 'casting-director-agent'
   - agentName: '选角导演 Agent'
   - agentDescription: '根据艺术总监的角色描述，生成三视图图像提示词（正面、侧面、动作姿态）'
   - templateId: 'builtin-casting-director-v1'
   - name: '内置默认模板 v1'
   - editablePart, lockedPart, userPromptTemplate 引用上述常量
   - systemPrompt getter 合并 editablePart 和 lockedPart

参考 artDirectorTemplates.ts 的代码风格，使用相同的注释格式和导出方式。
  </action>
  <verify>
    <automated>grep -n "CASTING_DIRECTOR_AGENT_EDITABLE_PART\|CASTING_DIRECTOR_AGENT_LOCKED_PART\|CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE\|CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE" src/shared/constants/castingDirectorTemplates.ts | head -20</automated>
  </verify>
  <done>
    - 文件 src/shared/constants/castingDirectorTemplates.ts 已创建
    - 包含 EDITABLE、LOCKED、DYNAMIC 三层定义
    - 导出 CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE 元数据对象
    - 代码风格与 artDirectorTemplates.ts 一致
  </done>
</task>

</tasks>

<verification>
1. 文件结构检查：确认文件包含文件头注释、三层提示词定义、元数据导出
2. 导出检查：确认所有四个常量都已导出（EDITABLE_PART, LOCKED_PART, USER_PROMPT_TEMPLATE, BUILTIN_TEMPLATE）
3. 模式一致性：确认代码风格与现有模板文件一致
</verification>

<success_criteria>
- src/shared/constants/castingDirectorTemplates.ts 文件存在且可编译
- 文件导出 CASTING_DIRECTOR_AGENT_EDITABLE_PART、CASTING_DIRECTOR_AGENT_LOCKED_PART、CASTING_DIRECTOR_AGENT_USER_PROMPT_TEMPLATE、CASTING_DIRECTOR_AGENT_BUILTIN_TEMPLATE
- BUILTIN_TEMPLATE 包含正确的 agentId、agentName、agentDescription
- systemPrompt getter 正确合并 editablePart 和 lockedPart
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent/02-01-SUMMARY.md`
</output>
