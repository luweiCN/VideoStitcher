---
phase: 02.5-agent
plan: 03
type: execute
wave: 2
depends_on:
  - 02.5-01-PLAN.md
  - 02.5-02-PLAN.md
files_modified:
  - src/shared/constants/promptTemplates.ts
  - src/main/ai/workflows/nodes/casting-director.ts
autonomous: true
requirements:
  - CASTM-04
  - CASTM-05
must_haves:
  truths:
    - "CASTING_PLANNER_AGENT 和 CASTING_VISUALIZER_AGENT 已注册到 BUILTIN_PROMPT_TEMPLATES"
    - "casting-director.ts Node 支持多阶段调用"
  artifacts:
    - path: "src/shared/constants/promptTemplates.ts"
      provides: "BUILTIN_PROMPT_TEMPLATES 注册多阶段选角导演"
    - path: "src/main/ai/workflows/nodes/casting-director.ts"
      provides: "LangGraph Node 多阶段调用逻辑"
  key_links:
    - from: "BUILTIN_PROMPT_TEMPLATES"
      to: "casting-director.ts Node"
      via: "runCastingDirectorAgent({ useMultiStage: true })"
---

<objective>
在 BUILTIN_PROMPT_TEMPLATES 中注册选角导演多阶段 Agent，并更新 LangGraph Node 支持多阶段调用。

Purpose: 让 PromptStudio 可以分别配置 Planner 和 Visualizer，让工作流可以使用多阶段选角导演。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02.5-agent-casting-multi/02.5-CONTEXT.md

## 参考实现

- `src/shared/constants/promptTemplates.ts` 中分镜设计 Agent 的注册方式
- `src/main/ai/workflows/nodes/storyboard-artist.ts` 多阶段 Node 实现

## 注册内容

### BUILTIN_PROMPT_TEMPLATES 新增

1. **casting-director-planner**
   - agentId: 'casting-director-planner'
   - agentName: '选角导演 - 规划器'
   - defaultModel: 'default'
   - supportedModelTypes: ['llm']

2. **casting-director-visualizer**
   - agentId: 'casting-director-visualizer'
   - agentName: '选角导演 - 可视化器'
   - defaultModel: 'default'
   - supportedModelTypes: ['image_generation']

## Node 更新

casting-director.ts Node 需要：
1. 检测配置中的 useMultiStage 选项
2. 调用 runCastingDirectorAgent({ useMultiStage: true })
3. 在两个阶段之间传递数据
4. 支持导演模式的两次检查
</context>

<tasks>

<task type="auto">
  <name>注册 BUILTIN_PROMPT_TEMPLATES</name>
  <read_first>
    - src/shared/constants/promptTemplates.ts
    - src/shared/constants/castingDirectorMultiTemplates.ts（已创建）
  </read_first>
  <files>src/shared/constants/promptTemplates.ts</files>
  <action>
更新 `src/shared/constants/promptTemplates.ts`：

1. **导入新模板**：
   ```typescript
   import {
     CASTING_PLANNER_AGENT_EDITABLE_PART,
     CASTING_PLANNER_AGENT_LOCKED_PART,
     CASTING_PLANNER_AGENT_USER_PROMPT_TEMPLATE,
     CASTING_PLANNER_AGENT_BUILTIN_TEMPLATE,
     CASTING_VISUALIZER_AGENT_EDITABLE_PART,
     CASTING_VISUALIZER_AGENT_LOCKED_PART,
     CASTING_VISUALIZER_AGENT_USER_PROMPT_TEMPLATE,
     CASTING_VISUALIZER_AGENT_BUILTIN_TEMPLATE,
   } from './castingDirectorMultiTemplates';
   ```

2. **添加到 BUILTIN_PROMPT_TEMPLATES 数组**：
   - 添加 casting-director-planner（在现有 casting-director-agent 之后）
   - 添加 casting-director-visualizer
   - 确保 model 配置正确（Planner 用 llm，Visualizer 用 image_generation）
  </action>
  <verify>
    <automated>grep -n "casting-director-planner\|casting-director-visualizer" src/shared/constants/promptTemplates.ts | head -10</automated>
  </verify>
</task>

<task type="auto">
  <name>更新 casting-director.ts Node</name>
  <read_first>
    - src/main/ai/workflows/nodes/casting-director.ts
    - src/main/ai/workflows/nodes/storyboard-artist.ts（多阶段参考）
  </read_first>
  <files>src/main/ai/workflows/nodes/casting-director.ts</files>
  <action>
更新 `src/main/ai/workflows/nodes/casting-director.ts`：

1. **导入更新**：
   - 从 casting-director/index.ts 导入 runCastingPlannerAgent 和 runCastingVisualizerAgent

2. **添加 useMultiStage 选项检测**：
   - 从 state.config 或 state.castingDirectorOptions 读取 useMultiStage
   - 默认保持 false（兼容现有行为）

3. **更新节点逻辑**：
   - 当 useMultiStage=true 时：
     * 先调用 runCastingPlannerAgent 生成视觉规格
     * 将结果存入 state.characterVisualSpecs
     * 再调用 runCastingVisualizerAgent 生成图像
   - 当 useMultiStage=false 时：
     * 保持现有单阶段调用逻辑

4. **导演模式支持**：
   - 两个阶段都支持 humanApproval 暂停
   - Planner 完成后检查是否暂停
   - Visualizer 完成后检查是否暂停
  </action>
  <verify>
    <automated>grep -n "useMultiStage\|runCastingPlannerAgent\|runCastingVisualizerAgent" src/main/ai/workflows/nodes/casting-director.ts | head -10</automated>
  </verify>
</task>

</tasks>

<verification>
1. 注册检查：确认 BUILTIN_PROMPT_TEMPLATES 包含两个阶段
2. Node 检查：确认 casting-director.ts 支持 useMultiStage 选项
3. 兼容检查：确认默认行为不变（单阶段）
</verification>

<success_criteria>
- BUILTIN_PROMPT_TEMPLATES 注册了 casting-director-planner 和 casting-director-visualizer
- casting-director.ts Node 支持 useMultiStage 选项
- PromptStudio 可以分别配置两个阶段
- 默认保持单阶段行为（向后兼容）
</success_criteria>
