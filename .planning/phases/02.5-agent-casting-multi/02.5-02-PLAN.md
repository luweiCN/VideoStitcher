---
phase: 02.5-agent
plan: 02
type: execute
wave: 1
depends_on:
  - 02.5-01-PLAN.md
files_modified:
  - src/main/ai/agents/casting-director/index.ts
autonomous: true
requirements:
  - CASTM-03
must_haves:
  truths:
    - "casting-director/index.ts 实现 runCastingPlannerAgent 函数"
    - "casting-director/index.ts 实现 runCastingVisualizerAgent 函数"
    - "runCastingDirectorAgent 函数可选择调用多阶段或单阶段版本"
  artifacts:
    - path: "src/main/ai/agents/casting-director/index.ts"
      provides: "选角导演多阶段 Agent 实现"
      exports:
        - "runCastingPlannerAgent"
        - "runCastingVisualizerAgent"
        - "runCastingDirectorAgent"
  key_links:
    - from: "runCastingPlannerAgent"
      to: "runCastingVisualizerAgent"
      via: "runCastingDirectorAgent 编排调用"
---

<objective>
创建选角导演多阶段 Agent 实现，包含 Planner 和 Visualizer 两个阶段的独立函数。

Purpose: 实现可在 LangGraph Node 中调用的多阶段选角导演 Agent。
Output: `src/main/ai/agents/casting-director/index.ts` 文件（更新或创建）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02.5-agent-casting-multi/02.5-CONTEXT.md

## 参考实现

- `src/main/ai/agents/storyboard-artist/index.ts` — 分镜设计多阶段实现参考
- `src/main/ai/agents/art-director/index.ts` — 单阶段 Agent 实现参考

## 接口定义

### Planner 输入
```typescript
interface CastingPlannerInput {
  artDirectorOutput: ArtDirectorOutput;
  scriptContent: string;
}
```

### Planner 输出
```typescript
interface CastingPlannerOutput {
  characterVisualSpecs: CharacterVisualSpec[];
  styleGuide: StyleGuide;
}
```

### Visualizer 输入
```typescript
interface CastingVisualizerInput {
  characterVisualSpecs: CharacterVisualSpec[];
  styleGuide: StyleGuide;
}
```

### Visualizer 输出
```typescript
interface CastingVisualizerOutput {
  imagePrompt: string;
  characterReferenceSheet: string; // URL
  styleNotes: string;
}
```
</context>

<tasks>

<task type="auto">
  <name>实现 casting-director 多阶段 Agent</name>
  <read_first>
    - src/main/ai/agents/storyboard-artist/index.ts
    - src/main/ai/agents/art-director/index.ts
  </read_first>
  <files>src/main/ai/agents/casting-director/index.ts</files>
  <action>
更新或创建 `src/main/ai/agents/casting-director/index.ts` 文件，包含：

1. **类型定义**：
   - CastingPlannerInput, CastingPlannerOutput
   - CastingVisualizerInput, CastingVisualizerOutput
   - CharacterVisualSpec, StyleGuide

2. **runCastingPlannerAgent 函数**：
   - 参数：input, options
   - 加载 Planner 提示词模板（CASTING_PLANNER_AGENT_BUILTIN_TEMPLATE）
   - 调用 LLM 生成角色视觉规格
   - 解析 JSON 输出
   - 返回 CastingPlannerOutput

3. **runCastingVisualizerAgent 函数**：
   - 参数：input, options
   - 加载 Visualizer 提示词模板（CASTING_VISUALIZER_AGENT_BUILTIN_TEMPLATE）
   - 调用 LLM 生成图像生成提示词
   - 调用图像生成 API（generateImage）
   - 返回 CastingVisualizerOutput

4. **runCastingDirectorAgent 函数（更新）**：
   - 新增 options.useMultiStage?: boolean（默认 false，保持兼容）
   - 当 useMultiStage=true 时：
     * 先调用 runCastingPlannerAgent
     * 再调用 runCastingVisualizerAgent
   - 当 useMultiStage=false 时：
     * 保持现有单阶段逻辑

5. **导演模式支持**：
   - 两个新函数都支持 options.directorMode
   - 在关键节点返回 humanApproval 暂停信号
  </action>
  <verify>
    <automated>grep -n "runCastingPlannerAgent\|runCastingVisualizerAgent" src/main/ai/agents/casting-director/index.ts | head -10</automated>
  </verify>
</task>

</tasks>

<verification>
1. 类型定义检查：确认所有输入输出类型已定义
2. 函数导出检查：确认三个函数都已导出
3. 兼容模式检查：确认 useMultiStage=false 时保持原有行为
</verification>

<success_criteria>
- src/main/ai/agents/casting-director/index.ts 实现三个函数
- runCastingPlannerAgent 正确生成角色视觉规格
- runCastingVisualizerAgent 正确生成图像和提示词
- 单阶段模式保持向后兼容
</success_criteria>
