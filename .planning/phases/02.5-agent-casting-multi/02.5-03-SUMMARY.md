---
phase: 02.5-agent
plan: 03
type: summary
subsystem: ai-agent
requirements:
  - CASTM-04
  - CASTM-05
dependencies:
  requires:
    - 02.5-01-PLAN.md
    - 02.5-02-PLAN.md
  provides:
    - BUILTIN_PROMPT_TEMPLATES 多阶段注册
    - LangGraph Node 多阶段调用支持
tech-stack:
  patterns:
    - 三层架构提示词
    - 多阶段 Agent 模式
key-files:
  created: []
  modified:
    - src/shared/constants/promptTemplates.ts
    - src/main/ai/workflows/nodes/casting-director.ts
    - src/main/ai/workflows/state.ts
decisions:
  - 保持向后兼容：useMultiStage 默认 false
  - 类型安全：在 WorkflowState 中定义 castingDirectorOptions
  - 导演模式在 Node 层统一处理
---

# Phase 2.5 Plan 03: 注册 BUILTIN_PROMPT_TEMPLATES 并更新 LangGraph Node

## 执行摘要

在 BUILTIN_PROMPT_TEMPLATES 中注册选角导演多阶段 Agent，并更新 LangGraph Node 支持多阶段调用。

## 任务完成情况

| 任务 | 状态 | 提交 |
|------|------|------|
| 注册 BUILTIN_PROMPT_TEMPLATES | 已完成 | (已有) |
| 更新 casting-director.ts Node | 已完成 | 5e017b2 |
| 添加 WorkflowState 类型定义 | 已完成 | fbd1712 |

## 变更详情

### 1. BUILTIN_PROMPT_TEMPLATES 注册

已在 `src/shared/constants/promptTemplates.ts` 中注册两个阶段：

- **casting-director-planner** (line 265)
  - agentName: '选角导演 - 规划器'
  - supportedModelTypes: ['llm']
  - 使用 CASTING_PLANNER_AGENT 三层提示词

- **casting-director-visualizer** (line 282)
  - agentName: '选角导演 - 可视化器'
  - supportedModelTypes: ['image_generation']
  - 使用 CASTING_VISUALIZER_AGENT 三层提示词

### 2. WorkflowState 类型扩展

在 `src/main/ai/workflows/state.ts` 中添加：

```typescript
castingDirectorOptions?: {
  useMultiStage?: boolean;
  plannerModelId?: string;
  visualizerModelId?: string;
};
```

### 3. Node 多阶段支持

在 `src/main/ai/workflows/nodes/casting-director.ts` 中：

- 检测 `state.castingDirectorOptions?.useMultiStage` 选项
- 传递给 `runCastingDirectorAgent` 的 agentOptions
- 支持新的 `character_reference_sheet` 结果格式

## 验证结果

```bash
# 注册检查
$ grep -n "casting-director-planner\|casting-director-visualizer" src/shared/constants/promptTemplates.ts
265:    agentId: 'casting-director-planner',
282:    agentId: 'casting-director-visualizer',

# Node 检查
$ grep -n "useMultiStage" src/main/ai/workflows/nodes/casting-director.ts
54:      useMultiStage: state.castingDirectorOptions?.useMultiStage ?? false,
```

## 向后兼容性

- `useMultiStage` 默认为 `false`，保持单阶段行为
- 现有工作流无需修改即可继续运行
- 新功能通过 `castingDirectorOptions` 配置对象启用

## 下一步

Phase 2.5 全部完成：
- 02.5-01: 创建 castingDirectorMultiTemplates.ts 三层提示词
- 02.5-02: 实现 casting-director 多阶段 Agent
- 02.5-03: 注册 BUILTIN_PROMPT_TEMPLATES 并更新 Node

接下来将进入 Phase 4：摄像师 Agent 重构。

## Self-Check: PASSED

- [x] BUILTIN_PROMPT_TEMPLATES 包含 casting-director-planner
- [x] BUILTIN_PROMPT_TEMPLATES 包含 casting-director-visualizer
- [x] casting-director.ts Node 支持 useMultiStage 选项
- [x] WorkflowState 包含 castingDirectorOptions 类型定义
- [x] 默认行为保持向后兼容
