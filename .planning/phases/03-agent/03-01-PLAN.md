---
phase: 03-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/constants/storyboardArtistTemplates.ts
autonomous: true
requirements:
  - STORY-01
  - STORY-02
must_haves:
  truths:
    - "分镜设计 Agent 提示词已拆分为 EDITABLE、LOCKED、DYNAMIC 三层"
    - "提示词常量文件可被 main 和 renderer 进程同时引用"
    - "STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE 元数据对象已导出"
  artifacts:
    - path: "src/shared/constants/storyboardArtistTemplates.ts"
      provides: "三层提示词定义和元数据导出"
      exports:
        - "STORYBOARD_ARTIST_AGENT_EDITABLE_PART"
        - "STORYBOARD_ARTIST_AGENT_LOCKED_PART"
        - "STORYBOARD_ARTIST_AGENT_USER_PROMPT_TEMPLATE"
        - "STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE"
  key_links:
    - from: "STORYBOARD_ARTIST_AGENT_EDITABLE_PART"
      to: "STORYBOARD_ARTIST_AGENT_LOCKED_PART"
      via: "STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE.systemPrompt getter"
---

<objective>
创建分镜设计 Agent 的三层提示词常量文件，遵循与艺术总监、选角导演 Agent 相同的架构模式。

Purpose: 将分镜设计 Agent 的提示词系统改造为可在 PromptStudio 中自定义的三层架构（EDITABLE + LOCKED + DYNAMIC）。
Output: `src/shared/constants/storyboardArtistTemplates.ts` 文件，包含完整的提示词定义和元数据导出。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent/03-CONTEXT.md

## 参考实现（必须遵循的模式）

### 文件结构参考
- `src/shared/constants/artDirectorTemplates.ts` — 三层架构模板定义的标准模式
- `src/shared/constants/castingDirectorTemplates.ts` — 含图像生成的 Agent 模板参考

### 关键模式
1. **EDITABLE 层**: Agent 人设、镜头语言词汇、分镜创作方法论、场景连贯性规则 —— 面向游戏行业专家，可在 PromptStudio 中自定义
2. **LOCKED 层**: 5x5 网格布局、无白边约束、4K 分辨率、JSON 输出格式 —— 代码解析依赖，不可修改
3. **DYNAMIC 层**: 运行时注入的上下文变量（剧本内容、角色参考图、场景描述、视频规格）

### 用户决策（来自 CONTEXT.md）
- **D-01**: EDITABLE 层包含镜头语言词汇、分镜创作方法论、场景连贯性规则、剧情节奏标记
- **D-02**: LOCKED 层包含 5x5 网格布局、禁止白边/间隙约束、4K 分辨率、JSON 输出格式定义
- **D-03**: DYNAMIC 层包含角色参考图 URL、视觉风格标签、剧本内容、视频规格

### 变量清单
```
{{scriptContent}} - 剧本内容
{{characterReferenceSheet}} - 选角导演输出（含 image_url, style_guide）
{{sceneBreakdowns}} - 场景描述
{{videoSpec}} - 视频规格（aspectRatio, duration）
{{artDirectorOutput}} - 艺术总监输出
```

### 图像生成提示词模板（LOCKED 层强制约束）
```typescript
const STORYBOARD_IMAGE_PROMPT = `Professional storyboard grid, exactly 5 rows and 5 columns of 25 panels,
{{layout_hint}}, zero gaps between panels, no borders no padding no margins no gutters no grid lines no black bars no white space, all panels flush edge-to-edge filling the entire image,
{{style_tags}}, each panel shows: {{frame_descriptions}},
consistent character design, sequential narrative flow, no text, no numbers`;

// layout_hint 动态插入
const isPortrait = aspectRatio === '9:16';
const layoutHint = isPortrait
  ? 'portrait orientation 9:16 vertical layout, each cell is taller than wide'
  : 'landscape orientation 16:9 horizontal layout, each cell is wider than tall';
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 storyboardArtistTemplates.ts 三层提示词常量文件</name>
  <read_first>
    - src/shared/constants/artDirectorTemplates.ts
    - src/shared/constants/castingDirectorTemplates.ts
    - src/main/ai/prompts/storyboard-artist-agent.ts
  </read_first>
  <files>src/shared/constants/storyboardArtistTemplates.ts</files>
  <action>
创建 `src/shared/constants/storyboardArtistTemplates.ts` 文件，包含以下内容：

1. **文件头注释**：说明文件用途（同时被 main 和 renderer 进程引用）和分层设计

2. **EDITABLE 层（STORYBOARD_ARTIST_AGENT_EDITABLE_PART）**：
   - Agent 人设定义：分镜设计总监，专注于将剧本转化为视觉分镜序列
   - 镜头语言词汇表：
     * 景别：close-up, medium shot, wide shot, extreme close-up, establishing shot
     * 角度：high angle, low angle, bird's eye view, eye level, dutch angle
     * 运动：static, pan, tilt, zoom, tracking, dolly, handheld
   - 分镜创作方法论：
     * 剧本段落 → 镜头序列的转换逻辑
     * 角色出场、冲突、高潮的视觉呈现
     * 转场效果选择（cut, fade, dissolve）
   - 场景连贯性规则：
     * 180度规则
     * 视线匹配
     * 动作连贯
   - 剧情节奏标记（供摄像师参考切分位置）：
     * 场景切换点识别（is_scene_change）
     * 高潮点标记（is_climax）
     * 过渡点识别（is_transition）

3. **LOCKED 层（STORYBOARD_ARTIST_AGENT_LOCKED_PART）**：
   - 5x5 网格布局约束（强制不可修改）
   - 禁止白边/间隙约束（zero gaps, no borders, no padding, no margins, no gutters, no grid lines, no black bars, no white space）
   - 4K 分辨率规格：
     * 横版（16:9）→ 3840x2160，每帧 768x432
     * 竖版（9:16）→ 2160x3840，每帧 432x768
   - 图像生成提示词模板（强制格式）
   - JSON 输出格式定义（严格遵守）
   - 字段约束：
     * storyboard_grid_image: string — 完整 5x5 分镜网格图 URL
     * frame_images: string[] — 25 张切割后的单帧图 base64
     * frames: 每帧元数据数组
     * style_notes: string — 整体风格描述
   - 自检清单

4. **DYNAMIC 层（STORYBOARD_ARTIST_AGENT_USER_PROMPT_TEMPLATE）**：
   - 使用变量占位符：{{scriptContent}}, {{characterReferenceSheet}}, {{sceneBreakdowns}}, {{videoSpec}}, {{artDirectorOutput}}
   - 模板结构包含：剧本内容、角色参考图信息、场景描述、视频规格
   - 明确要求生成 25 帧分镜（5x5 网格）
   - 要求为每帧添加剧情节奏标记

5. **元数据导出（STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE）**：
   - agentId: 'storyboard-artist-agent'
   - agentName: '分镜设计 Agent'
   - agentDescription: '根据剧本和角色参考图，生成 5x5 分镜网格图和 25 张单帧图'
   - templateId: 'builtin-storyboard-artist-v1'
   - name: '内置默认模板 v1'
   - editablePart, lockedPart, userPromptTemplate 引用上述常量
   - systemPrompt getter 合并 editablePart 和 lockedPart

参考 artDirectorTemplates.ts 的代码风格，使用相同的注释格式和导出方式。
  </action>
  <verify>
    <automated>grep -n "STORYBOARD_ARTIST_AGENT_EDITABLE_PART\|STORYBOARD_ARTIST_AGENT_LOCKED_PART\|STORYBOARD_ARTIST_AGENT_USER_PROMPT_TEMPLATE\|STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE" src/shared/constants/storyboardArtistTemplates.ts | head -20</automated>
  </verify>
  <done>
    - 文件 src/shared/constants/storyboardArtistTemplates.ts 已创建
    - 包含 EDITABLE、LOCKED、DYNAMIC 三层定义
    - 导出 STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE 元数据对象
    - 代码风格与 artDirectorTemplates.ts 一致
  </done>
</task>

</tasks>

<verification>
1. 文件结构检查：确认文件包含文件头注释、三层提示词定义、元数据导出
2. 导出检查：确认所有四个常量都已导出（EDITABLE_PART, LOCKED_PART, USER_PROMPT_TEMPLATE, BUILTIN_TEMPLATE）
3. 模式一致性：确认代码风格与现有模板文件一致
4. LOCKED 层检查：确认包含 5x5 网格、无白边约束、4K 分辨率、JSON 格式定义
</verification>

<success_criteria>
- src/shared/constants/storyboardArtistTemplates.ts 文件存在且可编译
- 文件导出 STORYBOARD_ARTIST_AGENT_EDITABLE_PART、STORYBOARD_ARTIST_AGENT_LOCKED_PART、STORYBOARD_ARTIST_AGENT_USER_PROMPT_TEMPLATE、STORYBOARD_ARTIST_AGENT_BUILTIN_TEMPLATE
- BUILTIN_TEMPLATE 包含正确的 agentId: 'storyboard-artist-agent'
- systemPrompt getter 正确合并 editablePart 和 lockedPart
- LOCKED 层包含完整的 5x5 网格约束和图像生成提示词模板
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent/03-01-SUMMARY.md`
</output>
