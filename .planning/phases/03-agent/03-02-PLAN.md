---
phase: 03-agent
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/main/ai/agents/storyboard-artist/index.ts
autonomous: true
requirements:
  - STORY-03
must_haves:
  truths:
    - "分镜设计 Agent 实现目录已创建"
    - "runStoryboardArtistAgent 主函数已导出"
    - "类型定义 StoryboardArtistResult、StoryboardArtistContext、StoryboardArtistAgentOptions 已导出"
    - "提示词构建函数 buildSystemPrompt、buildUserPrompt 已实现"
    - "输出解析函数 parseOutput 已实现"
    - "图像生成和 sharp 切割逻辑已集成"
  artifacts:
    - path: "src/main/ai/agents/storyboard-artist/index.ts"
      provides: "Agent 完整实现（含图像生成和切割）"
      exports:
        - "runStoryboardArtistAgent"
        - "StoryboardArtistResult"
        - "StoryboardArtistContext"
        - "StoryboardArtistAgentOptions"
      contains:
        - "function buildSystemPrompt"
        - "function buildUserPrompt"
        - "function parseOutput"
        - "provider.generateImage()"
        - "sharp 切割逻辑"
  key_links:
    - from: "buildSystemPrompt"
      to: "STORYBOARD_ARTIST_AGENT_EDITABLE_PART + STORYBOARD_ARTIST_AGENT_LOCKED_PART"
      via: "模板常量导入和字符串合并"
    - from: "runStoryboardArtistAgent"
      to: "getGlobalProvider().generateText()"
      via: "provider-manager 调用 LLM 生成分镜 JSON"
    - from: "runStoryboardArtistAgent"
      to: "getGlobalProvider().generateImage()"
      via: "provider-manager 调用图像生成"
    - from: "runStoryboardArtistAgent"
      to: "sharp 切割"
      via: "动态导入 sharp 并切割 5x5 网格为 25 张单帧"
    - from: "parseOutput"
      to: "StoryboardArtistResult"
      via: "JSON 解析和字段验证"
---

<objective>
创建分镜设计 Agent 的实现目录和主文件，实现完整的 Agent 调用逻辑，包括 LLM 调用、图像生成和 sharp 切割。

Purpose: 将分镜设计从 Node 中的实现迁移到独立的 Agent 模块，内部完成 LLM 生成分镜描述、图像生成、sharp 切割为 25 张单帧图的完整流程。
Output: `src/main/ai/agents/storyboard-artist/index.ts` 文件，包含类型定义、提示词构建、输出解析、图像生成、sharp 切割和主函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent/03-CONTEXT.md

## 参考实现（必须遵循的模式）

### Agent 实现结构参考
- `src/main/ai/agents/art-director/index.ts` — Agent 实现标准结构
- `src/main/ai/agents/casting-director/index.ts` — 含图像生成的 Agent 参考（LLM + generateImage）

### 关键模式
1. **类型定义区**: StoryboardArtistResult, StoryboardArtistContext, StoryboardArtistAgentOptions
2. **提示词构建区**: buildSystemPrompt(), buildUserPrompt()
3. **输出解析区**: parseOutput()
4. **Agent 主函数**: runStoryboardArtistAgent() — 包含 LLM 调用、图像生成、sharp 切割

### 用户决策（来自 CONTEXT.md）
- **D-04**: Agent 内部完成图像生成：调用 LLM 生成分镜描述（JSON 格式）→ 调用 generateImage 生成 5x5 分镜网格图 → 使用 sharp 切割为 25 张单帧图 → 返回完整结果（包含大图 + 小图数组）
- **D-05**: 修复横竖版 bug：横版（16:9）→ 3840x2160，竖版（9:16）→ 2160x3840
- **D-06**: 分镜师只负责生成分镜，不负责视频分组（分组由摄像师 Agent 处理）
- **D-07**: 导演模式两次 humanApproval：LLM 后 + 图像生成后
- **D-08**: 标准目录结构 `src/main/ai/agents/storyboard-artist/index.ts`

### 输出 JSON 结构
```typescript
interface StoryboardArtistResult {
  // 完整 5x5 分镜网格图（支持参考图的模型使用）
  storyboard_grid_image: string;

  // 25 张切割后的单帧图（只支持首尾帧的模型使用）
  frame_images: string[];

  // 每帧详细元数据
  frames: {
    frame_number: number;
    description: string;
    duration: number;
    shot_type: string;
    character_refs: string[];
    camera_movement: string;
    is_key_frame: boolean;
    narrative_beats?: {
      is_scene_change: boolean;
      is_climax: boolean;
      is_transition: boolean;
    }
  }[];

  // 整体风格描述（供摄像师传递给视频模型）
  style_notes: string;
}
```

### 输入上下文结构
```typescript
interface StoryboardArtistContext {
  artDirectorOutput: ArtDirectorResult;      // 艺术总监输出
  castingDirectorOutput: CastingDirectorResult; // 选角导演输出（含 image_url, style_guide）
  scriptContent: string;                     // 剧本内容
  videoSpec: { aspectRatio: '9:16' | '16:9'; duration: number };
}
```

### Sharp 切割逻辑参考（来自 storyboard-artist.ts Node）
```typescript
// 动态导入 sharp
const sharp = await import('sharp');

// 获取图片实际尺寸
const gridMeta = await sharp(localGridPath).metadata();
const rawWidth: number = gridMeta.width || 2560;
const rawHeight: number = gridMeta.height || 1440;

// 自动裁剪边框：对每条边最多裁剪 3%，然后取整到能被 5 整除的值
const maxCropRatio = 0.03;
const cropPx = (dim: number) => Math.floor(dim * maxCropRatio);
const cropLeft = cropPx(rawWidth);
const cropTop = cropPx(rawHeight);
const croppedWidth = Math.floor((rawWidth - cropLeft * 2) / 5) * 5;
const croppedHeight = Math.floor((rawHeight - cropTop * 2) / 5) * 5;
const frameWidth = croppedWidth / 5;
const frameHeight = croppedHeight / 5;

// 切割 25 帧并转 base64（按行优先：第1行5帧，第2行5帧...）
for (let row = 0; row < 5; row++) {
  for (let col = 0; col < 5; col++) {
    const frameBuffer = await sharp(localGridPath)
      .extract({
        left: cropLeft + col * frameWidth,
        top: cropTop + row * frameHeight,
        width: frameWidth,
        height: frameHeight,
      })
      .jpeg({ quality: 85 })
      .toBuffer();
    frameBase64List.push(`data:image/jpeg;base64,${frameBuffer.toString('base64')}`);
  }
}
```

### 图像生成提示词构建
```typescript
const isPortrait = videoSpec.aspectRatio === '9:16';
const layoutHint = isPortrait
  ? 'portrait orientation 9:16 vertical layout, each cell is taller than wide'
  : 'landscape orientation 16:9 horizontal layout, each cell is wider than tall';

const styleTags = castingDirectorOutput.character_reference_sheet?.style_guide?.style_consistency_tags?.join(', ')
  || 'photorealistic, cinematic composition';

const frameDescriptions = frames.map((f, i) => `Frame ${i + 1}: ${f.description}`).join('. ');

const storyboardPrompt = `Professional storyboard grid, exactly 5 rows and 5 columns of 25 panels, ${layoutHint}, zero gaps between panels, no borders no padding no margins no gutters no grid lines no black bars no white space, all panels flush edge-to-edge filling the entire image, ${styleTags}, each panel shows: ${frameDescriptions}, consistent character design, sequential narrative flow, no text, no numbers`;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 storyboard-artist/index.ts Agent 实现</name>
  <read_first>
    - src/main/ai/agents/art-director/index.ts
    - src/main/ai/agents/casting-director/index.ts
    - src/main/ai/workflows/nodes/storyboard-artist.ts
    - src/shared/constants/storyboardArtistTemplates.ts
  </read_first>
  <files>src/main/ai/agents/storyboard-artist/index.ts</files>
  <action>
创建 `src/main/ai/agents/storyboard-artist/index.ts` 文件，包含以下内容：

1. **文件头注释**：说明 Agent 职责（分镜设计总监）和调用流程（LLM → 图像生成 → sharp 切割）

2. **导入语句**：
   - `import { STORYBOARD_ARTIST_AGENT_EDITABLE_PART, STORYBOARD_ARTIST_AGENT_LOCKED_PART, STORYBOARD_ARTIST_AGENT_USER_PROMPT_TEMPLATE } from '@shared/constants/storyboardArtistTemplates';`
   - `import { getGlobalProvider } from '../../provider-manager';`
   - `import { downloadToCache } from '@main/utils/cache';`
   - `import type { ArtDirectorResult } from '@shared/types/aside';`
   - `import type { CastingDirectorResult } from '../casting-director';`

3. **类型定义区**：
   - `StoryboardFrame` 接口：单帧元数据（frame_number, description, duration, shot_type, character_refs, camera_movement, is_key_frame, narrative_beats）
   - `StoryboardArtistResult` 接口：包含 storyboard_grid_image, frame_images, frames, style_notes
   - `StoryboardArtistContext` 接口：包含 artDirectorOutput, castingDirectorOutput, scriptContent, videoSpec
   - `StoryboardArtistAgentOptions` 接口：包含 modelId?: string, customEditablePart?: string, currentIndex?: number, totalCount?: number

4. **提示词构建区**：
   - `buildSystemPrompt(customEditablePart?: string): string` — 合并 EDITABLE + LOCKED
   - `buildUserPrompt(context: StoryboardArtistContext, currentIndex: number, totalCount: number, logger?): string` — 注入变量：
     * 使用 `JSON.stringify(context.artDirectorOutput, null, 2)` 填充 {{artDirectorOutput}}
     * 使用 `JSON.stringify(context.castingDirectorOutput, null, 2)` 填充 {{characterReferenceSheet}}
     * 使用 `context.scriptContent` 填充 {{scriptContent}}
     * 使用 `JSON.stringify(context.videoSpec, null, 2)` 填充 {{videoSpec}}
   - 日志输出（如果 logger 存在）：记录 systemPromptLength, userPromptLength

5. **输出解析区**：
   - `parseOutput(llmOutput: string): { frames: StoryboardFrame[], style_notes: string }` — 提取 JSON 代码块并解析
   - 验证必要字段：frames 必须存在且为数组，长度应为 25
   - 验证每帧包含：frame_number, description, duration, shot_type

6. **图像生成和切割区**：
   - `generateStoryboardImage(frames: StoryboardFrame[], castingDirectorOutput: CastingDirectorResult, videoSpec: VideoSpec, logger?): Promise<{ imageUrl: string, localPath: string }>`
     * 构建图像生成提示词（使用 layoutHint、styleTags、frameDescriptions）
     * 调用 `provider.generateImage()` 生成 5x5 网格图
     * 使用 `downloadToCache()` 下载图片到本地
     * 返回 imageUrl 和 localPath
   - `sliceStoryboardGrid(localPath: string, logger?): Promise<string[]>`
     * 动态导入 sharp：`const sharp = await import('sharp')`
     * 获取图片元数据，计算裁剪参数（最多裁剪 3% 边框）
     * 切割为 25 张单帧图（按行优先：row 0-4, col 0-4）
     * 每张图转为 base64 字符串（data:image/jpeg;base64,...）
     * 返回 25 个 base64 字符串数组

7. **Agent 主函数**：
   - `runStoryboardArtistAgent(context, options, logger?): Promise<StoryboardArtistResult>`
   - 步骤：
     1. 获取 provider（getGlobalProvider()）
     2. 构建提示词
     3. 调用 LLM：`provider.generateText(userPrompt, { systemPrompt, temperature: 0.7, maxTokens: 8192 })`
     4. 解析输出，获取 frames 和 style_notes
     5. 调用 generateStoryboardImage 生成分镜网格图
     6. 调用 sliceStoryboardGrid 切割为 25 张单帧图
     7. 返回完整结果

8. **默认导出**：`export default runStoryboardArtistAgent;`

代码风格必须与 casting-director/index.ts 完全一致，包括：
- 使用 `// ─── 类型定义 ─────────────────────────────────────────────` 分隔区
- 使用相同的日志输出格式
- 使用相同的错误处理方式
- 图像生成和 sharp 切割逻辑复用现有 Node 中的实现
  </action>
  <verify>
    <automated>grep -n "runStoryboardArtistAgent\|StoryboardArtistResult\|StoryboardArtistContext\|StoryboardArtistAgentOptions\|buildSystemPrompt\|buildUserPrompt\|parseOutput\|generateStoryboardImage\|sliceStoryboardGrid" src/main/ai/agents/storyboard-artist/index.ts | head -30</automated>
  </verify>
  <done>
    - 文件 src/main/ai/agents/storyboard-artist/index.ts 已创建
    - 导出 runStoryboardArtistAgent 主函数
    - 导出 StoryboardArtistResult、StoryboardArtistContext、StoryboardArtistAgentOptions 类型
    - 实现 buildSystemPrompt、buildUserPrompt、parseOutput 函数
    - 实现 generateStoryboardImage 函数（调用 provider.generateImage）
    - 实现 sliceStoryboardGrid 函数（使用 sharp 切割 5x5 网格）
    - 代码风格与 casting-director/index.ts 一致
  </done>
</task>

</tasks>

<verification>
1. 文件结构检查：确认文件包含类型定义、提示词构建、输出解析、图像生成、切割、主函数六个区域
2. 导出检查：确认主函数和类型都已导出
3. 导入检查：确认正确导入 storyboardArtistTemplates 常量、provider-manager、cache 工具
4. Sharp 切割检查：确认使用动态导入 sharp，实现 5x5 切割逻辑
5. 图像生成检查：确认调用 provider.generateImage 并正确处理返回结果
</verification>

<success_criteria>
- src/main/ai/agents/storyboard-artist/index.ts 文件存在且可编译
- 文件导出 runStoryboardArtistAgent、StoryboardArtistResult、StoryboardArtistContext、StoryboardArtistAgentOptions
- buildSystemPrompt 正确合并 EDITABLE + LOCKED
- buildUserPrompt 正确注入所有 DYNAMIC 变量
- parseOutput 正确解析 JSON 并返回 frames 和 style_notes
- generateStoryboardImage 正确调用 provider.generateImage() 并返回 imageUrl 和 localPath
- sliceStoryboardGrid 正确使用 sharp 切割 5x5 网格为 25 张单帧 base64
- 主函数按顺序调用 LLM、图像生成、sharp 切割，并返回完整结果
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent/03-02-SUMMARY.md`
</output>
