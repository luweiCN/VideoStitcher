---
phase: 04-agent
plan: 06
type: ui-fix
wave: 1
depends_on:
  - 04-05-PLAN.md
gap_closure: true
files_modified:
  - src/renderer/pages/AICreative/agentStudio/index.tsx
  - src/renderer/pages/AICreative/agentStudio/types.ts
  - src/shared/constants/promptTemplates.ts
autonomous: true
must_haves:
  truths:
    - "摄像师 Agent 不显示内置默认模板"
    - "摄像师 Agent 不显示'新建模板'按钮"
    - "摄像师 Agent 只显示模型选择区域"
  artifacts:
    - path: "src/renderer/pages/AICreative/agentStudio/index.tsx"
      provides: "简化摄像师 Agent 的 UI"
---

<objective>
针对摄像师 Agent 简化 PromptStudio UI：
1. 不显示内置默认模板
2. 不显示"新建模板"按钮
3. 只显示模型选择（文字模型 + 视频模型）

Purpose: 摄像师 Agent 的提示词是内置固定的，不需要用户编辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
当前问题：
- PromptStudio 显示摄像师 Agent 时，仍然显示"内置默认模板"区域
- 显示"新建模板"按钮
- 但这些对摄像师 Agent 没有意义（提示词已内置固定）

期望设计：
- 检测 agentId === 'cinematographer-agent'
- 隐藏提示词模板相关 UI
- 只显示模型选择区域
- 支持选择两个模型：文字模型 + 视频模型

需要在 promptTemplates.ts 中添加标记，标识哪些 Agent 使用内置提示词：
```typescript
builtinPrompts: true  // 新增字段
```
</context>

<tasks>

<task type="auto">
  <name>更新 BUILTIN_PROMPT_TEMPLATES 标记内置提示词</name>
  <read_first>
    - src/shared/constants/promptTemplates.ts
  </read_first>
  <files>src/shared/constants/promptTemplates.ts</files>
  <action>
为 cinematographer-agent 添加 builtinPrompts 标记：

```typescript
{
  agentId: 'cinematographer-agent',
  agentName: '摄像师 Agent',
  // ... 其他字段
  builtinPrompts: true,  // 新增：提示词内置，不可编辑
}
```

如果 types.ts 中没有这个字段，需要添加。
  </action>
  <verify>
    <automated>grep -n "builtinPrompts" src/shared/constants/promptTemplates.ts</automated>
  </verify>
  <done>
    - cinematographer-agent 配置了 builtinPrompts: true
  </done>
</task>

<task type="auto">
  <name>更新 PromptStudio UI 简化内置提示词 Agent</name>
  <read_first>
    - src/renderer/pages/AICreative/agentStudio/index.tsx
    - src/renderer/pages/AICreative/agentStudio/types.ts
  </read_first>
  <files>src/renderer/pages/AICreative/agentStudio/index.tsx</files>
  <action>
**Step 1: 读取 builtinPrompts 标记**

获取当前选中的 Agent 配置，检查 `builtinPrompts` 字段。

**Step 2: 条件渲染 UI**

当 `builtinPrompts === true` 时：
- 隐藏"内置默认模板"区域
- 隐藏"新建模板"按钮
- 显示简化提示："此 Agent 使用内置提示词，不可编辑"

**Step 3: 显示双模型选择**

对于摄像师 Agent，显示两个模型选择器：
- 文字模型（用于 Planner）
- 视频模型（用于视频生成）

代码示例：
```typescript
const isBuiltinPrompts = selectedAgent?.builtinPrompts;

// 模板列表区域
{!isBuiltinPrompts && (
  <>
    <h3>内置默认模板</h3>
    {/* 模板列表 */}
    <button>新建模板</button>
  </>
)}

{isBuiltinPrompts && (
  <div className="text-gray-500">
    此 Agent 使用内置提示词，不可编辑
  </div>
)}
```
  </action>
  <verify>
    <automated>grep -n "builtinPrompts\|isBuiltinPrompts" src/renderer/pages/AICreative/agentStudio/index.tsx | head -10</automated>
  </verify>
  <done>
    - 检测 builtinPrompts 标记
    - 内置提示词 Agent 隐藏模板 UI
    - 显示双模型选择（摄像师 Agent）
    - 使用中文文案
  </done>
</task>

</tasks>

<verification>
1. 选择摄像师 Agent 时不显示"内置默认模板"
2. 选择摄像师 Agent 时不显示"新建模板"按钮
3. 显示提示"此 Agent 使用内置提示词"
4. 显示文字模型和视频模型选择
5. 其他 Agent 正常显示模板 UI
</verification>

<success_criteria>
- 摄像师 Agent 不显示提示词模板相关 UI
- 只显示模型选择区域
- 其他 Agent 不受影响
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent/04-06-SUMMARY.md`
</output>
