---
phase: 04-agent
plan: 05
type: redesign
wave: 1
depends_on:
  - 04-04-PLAN.md
gap_closure: true
files_modified:
  - src/renderer/pages/AICreative/agentStudio/agents.ts
  - src/shared/constants/promptTemplates.ts
  - src/main/ai/workflows/nodes/cinematographer.ts
  - src/main/ai/agents/cinematographer/index.ts
autonomous: true
must_haves:
  truths:
    - "PromptStudio 只显示一个'摄像师 Agent'"
    - "提示词固定内置，PromptStudio 不可见不可编辑"
    - "只能选择文字模型和视频模型"
    - "内部自动根据模型能力选择工作流模式"
  artifacts:
    - path: "src/renderer/pages/AICreative/agentStudio/agents.ts"
      provides: "单个摄像师 Agent 配置"
    - path: "src/shared/constants/promptTemplates.ts"
      provides: " cinematographer-agent 配置（提示词内置）"
---

<objective>
重新设计摄像师 Agent：
1. PromptStudio 只显示一个「摄像师 Agent」
2. 提示词固定内置，不可见、不可编辑、不可添加模板
3. 只能选择两个模型：文字模型（Planner）和视频模型
4. 内部自动根据视频模型能力选择工作流模式

Purpose: 简化用户体验，摄像师是专业角色，提示词不应被修改。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
当前问题：
- agents.ts 有两个摄像师 Agent（规划器 + 执行器）
- BUILTIN_PROMPT_TEMPLATES 有两个配置
- 提示词在 PromptStudio 可见可编辑

期望设计：
- agents.ts 只有一个「摄像师 Agent」
- BUILTIN_PROMPT_TEMPLATES 只有一个配置
- 提示词内置在代码中，不在 PromptStudio 显示
- 模型选择：文字模型 + 视频模型

工作流逻辑：
```
用户选择视频模型
    ↓
检查模型能力 supportsReferenceImage
    ↓
支持参考图（如 Seedance）→ 单阶段：分镜图 → 视频
不支持参考图（如 Kling）→ 多阶段：Planner → 分段 → 切图 → 生成 → 拼接
```
</context>

<tasks>

<task type="auto">
  <name>简化 agents.ts 为一个摄像师 Agent</name>
  <read_first>
    - src/renderer/pages/AICreative/agentStudio/agents.ts
  </read_first>
  <files>src/renderer/pages/AICreative/agentStudio/agents.ts</files>
  <action>
删除两个摄像师 Agent（cinematographer-planner-agent 和 cinematographer-executor-agent）

添加一个统一的摄像师 Agent：

```typescript
{
  id: 'cinematographer-agent',
  name: '摄像师 Agent',
  role: '视频生成',
  description: '根据分镜输出和视频规格生成最终视频。自动根据所选视频模型能力选择最佳工作流：支持参考图的模型直接使用分镜图生成；只支持首尾帧的模型自动分段、切图、生成并拼接。',
  icon: Video,
  iconColor: 'text-orange-400',
  bgColor: 'bg-orange-500/10 group-hover:bg-orange-500',
  modelTypes: ['text', 'video'],  // 需要选择文字模型和视频模型
}
```

移除 Film 图标导入（如不再需要）。
  </action>
  <verify>
    <automated>grep -c "cinematographer" src/renderer/pages/AICreative/agentStudio/agents.ts</automated>
  </verify>
  <done>
    - agents.ts 只有一个 cinematographer-agent
    - modelTypes 包含 text 和 video
  </done>
</task>

<task type="auto">
  <name>更新 BUILTIN_PROMPT_TEMPLATES</name>
  <read_first>
    - src/shared/constants/promptTemplates.ts
    - src/shared/constants/cinematographerTemplates.ts
  </read_first>
  <files>src/shared/constants/promptTemplates.ts</files>
  <action>
**Step 1: 替换多阶段配置为单配置**

删除 cinematographer-planner 和 cinematographer-executor 两个模板配置。

添加统一的 cinematographer-agent 配置：

```typescript
{
  agentId: 'cinematographer-agent',
  agentName: '摄像师 Agent',
  description: '根据分镜输出生成视频，自动选择最佳工作流',
  defaultModel: 'default',
  supportedModelTypes: ['text', 'video'],  // 需要两种模型
  templateId: 'builtin-cinematographer-agent-v1',
  // 提示词不在这里定义，内置在 Agent 代码中
  editablePart: '',  // 空，不可编辑
  lockedPart: '',    // 空，内置在代码中
  userPromptTemplate: '',  // 空，由代码动态构建
}
```

**Step 2:  cinematographerTemplates.ts 改为内部使用**

该文件保留，但改为：
- 不导出到 BUILTIN_PROMPT_TEMPLATES
- 仅供 cinematographer/index.ts 内部导入使用
- 提示词作为实现细节，不对用户暴露
  </action>
  <verify>
    <automated>grep -n "cinematographer-agent" src/shared/constants/promptTemplates.ts | head -5</automated>
  </verify>
  <done>
    - BUILTIN_PROMPT_TEMPLATES 只有一个 cinematographer-agent
    - editablePart 和 lockedPart 为空
    - supportedModelTypes 包含 text 和 video
  </done>
</task>

<task type="auto">
  <name>更新 Node 支持双模型选择</name>
  <read_first>
    - src/main/ai/workflows/nodes/cinematographer.ts
    - src/main/ai/workflows/state.ts
  </read_first>
  <files>
    - src/main/ai/workflows/nodes/cinematographer.ts
    - src/main/ai/workflows/state.ts
  </files>
  <action>
**Step 1: 更新 WorkflowState 类型**

确保 state 可以存储两种模型配置：
```typescript
interface CinematographerOptions {
  textModel?: string;      // Planner 使用的文字模型
  videoModel: string;      // 视频生成模型
  videoModelConfig?: {
    supportsReferenceImage: boolean;
    maxDuration: number;
  };
}
```

**Step 2: 更新 cinematographer.ts Node**

修改节点逻辑：
1. 从 state 读取 textModel 和 videoModel
2. 根据 videoModel 的能力决定工作流模式
3. 调用 Agent 时传入两种模型配置

```typescript
// 读取模型配置
const textModel = state.config?.cinematographerOptions?.textModel ?? 'default';
const videoModel = state.config?.cinematographerOptions?.videoModel;
const videoModelConfig = state.config?.cinematographerOptions?.videoModelConfig;

// 自动决定工作流模式
const useMultiStage = !videoModelConfig?.supportsReferenceImage;

// 调用 Agent
const result = await runCinematographerAgent({
  storyboardOutput,
  videoSpec,
  textModel,
  videoModel,
  videoModelConfig,
  useMultiStage,  // 内部自动决定，非用户配置
}, provider, logger);
```
  </action>
  <verify>
    <automated>grep -n "textModel\|videoModel" src/main/ai/workflows/nodes/cinematographer.ts | head -10</automated>
  </verify>
  <done>
    - Node 支持读取 textModel 和 videoModel
    - Node 根据 videoModelConfig.supportsReferenceImage 决定模式
    - 使用中文日志
  </done>
</task>

<task type="auto">
  <name>更新 Agent 使用内置提示词</name>
  <read_first>
    - src/main/ai/agents/cinematographer/index.ts
    - src/shared/constants/cinematographerTemplates.ts
  </read_first>
  <files>src/main/ai/agents/cinematographer/index.ts</files>
  <action>
**Step 1: 修改函数签名**

更新 runCinematographerAgent 参数：
```typescript
interface CinematographerAgentInput {
  storyboardOutput: StoryboardOutput;
  videoSpec: VideoSpec;
  textModel: string;           // 文字模型（Planner 用）
  videoModel: string;          // 视频模型
  videoModelConfig: {
    supportsReferenceImage: boolean;
    maxDuration: number;
  };
  useMultiStage: boolean;      // 由调用方根据模型能力决定
}
```

**Step 2: 内置提示词**

从 cinematographerTemplates.ts 导入提示词模板，但：
- 不暴露给 PromptStudio
- 在 Agent 内部拼接使用
- 用户无法修改

```typescript
// 内置提示词，用户不可见
const PLANNER_SYSTEM_PROMPT = `...内置的 Planner 系统提示词...`;
const EXECUTOR_SYSTEM_PROMPT = `...内置的 Executor 系统提示词...`;
```

**Step 3: 根据模式选择工作流**

```typescript
if (input.useMultiStage) {
  // 多阶段：Planner（文字模型）→ Executor（视频模型）
  const renderPlan = await runPlannerWithTextModel(input, textModel);
  const video = await runExecutorWithVideoModel(renderPlan, input.videoModel);
  return video;
} else {
  // 单阶段：直接使用视频模型生成分镜图
  const video = await generateWithReferenceImage(input, input.videoModel);
  return video;
}
```
  </action>
  <verify>
    <automated>grep -n "PLANNER_SYSTEM_PROMPT\|EXECUTOR_SYSTEM_PROMPT" src/main/ai/agents/cinematographer/index.ts | head -5</automated>
  </verify>
  <done>
    - Agent 使用内置提示词（不依赖外部模板）
    - 支持 textModel 和 videoModel 分别指定
    - 根据 useMultiStage 选择工作流
  </done>
</task>

</tasks>

<verification>
1. agents.ts 只有一个「摄像师 Agent」
2. PromptStudio 不显示提示词编辑界面
3. 可以选择文字模型和视频模型
4. 内部自动根据视频模型能力选择工作流
5. TypeScript 编译无错误
6. 使用中文日志
</verification>

<success_criteria>
- PromptStudio 只显示一个「摄像师 Agent」
- 提示词不可见、不可编辑、不可添加模板
- 模型选择：文字模型 + 视频模型
- 支持参考图的模型（如 Seedance）直接使用分镜图生成
- 只支持首尾帧的模型（如 Kling）自动进入多阶段模式
- 所有提示词内置在代码中
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent/04-05-SUMMARY.md`
</output>
