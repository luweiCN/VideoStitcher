---
phase: 04-agent
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - src/shared/constants/promptTemplates.ts
  - src/main/ai/workflows/nodes/cinematographer.ts
autonomous: true
requirements:
  - CINE-04
  - CINE-05
must_haves:
  truths:
    - "CINEMATOGRAPHER_PLANNER_AGENT 和 CINEMATOGRAPHER_EXECUTOR_AGENT 已注册到 BUILTIN_PROMPT_TEMPLATES"
    - "cinematographer.ts Node 支持多阶段调用"
    - "Node 支持 useMultiStage 选项检测"
  artifacts:
    - path: "src/shared/constants/promptTemplates.ts"
      provides: "BUILTIN_PROMPT_TEMPLATES 注册多阶段摄像师"
    - path: "src/main/ai/workflows/nodes/cinematographer.ts"
      provides: "LangGraph Node 多阶段调用逻辑"
  key_links:
    - from: "BUILTIN_PROMPT_TEMPLATES"
      to: "cinematographer.ts Node"
      via: "runCinematographerAgent({ useMultiStage: true })"
---

<objective>
在 BUILTIN_PROMPT_TEMPLATES 中注册摄像师多阶段 Agent，并更新 LangGraph Node 支持多阶段调用。

Purpose: 让 PromptStudio 可以分别配置 Planner 和 Executor，让工作流可以使用多阶段摄像师 Agent。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-agent/04-CONTEXT.md

## 参考实现

- `src/shared/constants/promptTemplates.ts` 中分镜设计 Agent 的注册方式
- `src/main/ai/workflows/nodes/storyboard-artist.ts` 多阶段 Node 实现
- `src/main/ai/workflows/nodes/casting-director.ts` 多阶段 Node 实现

## 注册内容

### BUILTIN_PROMPT_TEMPLATES 新增

1. **cinematographer-planner**
   - agentId: 'cinematographer-planner'
   - agentName: '摄像师 - 规划器'
   - defaultModel: 'default'
   - supportedModelTypes: ['llm']

2. **cinematographer-executor**
   - agentId: 'cinematographer-executor'
   - agentName: '摄像师 - 执行器'
   - defaultModel: 'default'
   - supportedModelTypes: ['video_generation']

## Node 更新

cinematographer.ts Node 需要：
1. 检测配置中的 useMultiStage 选项
2. 调用 runCinematographerAgent({ useMultiStage: true })
3. 在两个阶段之间传递数据（RenderPlan）
4. 保持现有的 ffmpeg 拼接逻辑

## 决策参考

- D-01: 导演模式不设置人工确认
- D-07: 视频生成调用位置在 Agent 内部
</context>

<tasks>

<task type="auto">
  <name>注册 BUILTIN_PROMPT_TEMPLATES</name>
  <read_first>
    - src/shared/constants/promptTemplates.ts
    - src/shared/constants/cinematographerTemplates.ts（已创建）
  </read_first>
  <files>src/shared/constants/promptTemplates.ts</files>
  <action>
更新 `src/shared/constants/promptTemplates.ts`：

1. **导入新模板**：
   ```typescript
   import {
     CINEMATOGRAPHER_PLANNER_AGENT_EDITABLE_PART,
     CINEMATOGRAPHER_PLANNER_AGENT_LOCKED_PART,
     CINEMATOGRAPHER_PLANNER_AGENT_USER_PROMPT_TEMPLATE,
     CINEMATOGRAPHER_PLANNER_AGENT_BUILTIN_TEMPLATE,
     CINEMATOGRAPHER_EXECUTOR_AGENT_EDITABLE_PART,
     CINEMATOGRAPHER_EXECUTOR_AGENT_LOCKED_PART,
     CINEMATOGRAPHER_EXECUTOR_AGENT_USER_PROMPT_TEMPLATE,
     CINEMATOGRAPHER_EXECUTOR_AGENT_BUILTIN_TEMPLATE,
   } from './cinematographerTemplates';
   ```

2. **添加到 BUILTIN_PROMPT_TEMPLATES 数组**：
   - 添加 cinematographer-planner（在 storyboard-artist-agent 之后）
   - 添加 cinematographer-executor
   - 确保 model 配置正确（Planner 用 llm，Executor 用 video_generation）

3. **保持向后兼容**：
   - 保留现有的 cinematographer-agent 单阶段配置（如有）
   - 或添加兼容版本
  </action>
  <verify>
    <automated>grep -n "cinematographer-planner\|cinematographer-executor" src/shared/constants/promptTemplates.ts | head -10</automated>
  </verify>
  <done>
    - BUILTIN_PROMPT_TEMPLATES 包含 cinematographer-planner 和 cinematographer-executor
    - 导入语句正确
    - 模型类型配置正确
  </done>
</task>

<task type="auto">
  <name>更新 cinematographer.ts Node</name>
  <read_first>
    - src/main/ai/workflows/nodes/cinematographer.ts
    - src/main/ai/agents/cinematographer/index.ts（已创建）
    - src/main/ai/workflows/nodes/storyboard-artist.ts（多阶段参考）
  </read_first>
  <files>src/main/ai/workflows/nodes/cinematographer.ts</files>
  <action>
更新 `src/main/ai/workflows/nodes/cinematographer.ts`：

1. **导入更新**：
   - 从 cinematographer/index.ts 导入 runCinematographerAgent
   - 移除旧的 CinematographerAgentPrompts 导入（如不再需要）

2. **添加 useMultiStage 选项检测**：
   - 从 state.config 或 state.cinematographerOptions 读取 useMultiStage
   - 默认保持 false（兼容现有行为）

3. **重构节点逻辑**：
   - 当 useMultiStage=true 时：
     * 调用 runCinematographerAgent({ useMultiStage: true })
     * 传入 storyboardOutput, videoSpec, modelCapabilities
     * 获取最终视频 URL
   - 当 useMultiStage=false 时：
     * 保持现有逻辑（或调用 runCinematographerAgent({ useMultiStage: false })）

4. **简化 Node 代码**：
   - 将视频生成逻辑迁移到 Agent 内部（per D-07）
   - Node 只负责：
     * 准备输入数据
     * 调用 Agent
     * 处理输出结果
     * 更新 WorkflowState
   - 保留 ffmpeg 拼接逻辑（在 Agent 内部调用）

5. **导演模式支持**：
   - 根据 D-01，humanApproval = false（不暂停）
   - 如需支持导演模式，可添加暂停检查

6. **日志更新**：
   - 使用中文日志
   - 区分多阶段和单阶段模式的日志
  </action>
  <verify>
    <automated>grep -n "useMultiStage\|runCinematographerAgent" src/main/ai/workflows/nodes/cinematographer.ts | head -10</automated>
  </verify>
  <done>
    - Node 支持 useMultiStage 选项
    - 调用 runCinematographerAgent 进行视频生成
    - 代码简化，逻辑清晰
    - 使用中文日志
  </done>
</task>

</tasks>

<verification>
1. 注册检查：确认 BUILTIN_PROMPT_TEMPLATES 包含两个阶段
2. Node 检查：确认 cinematographer.ts 支持 useMultiStage 选项
3. 导入检查：确认正确导入 cinematographer/index.ts
4. 兼容检查：确认默认行为不变（单阶段）
5. 日志检查：确认使用中文日志
</verification>

<success_criteria>
- BUILTIN_PROMPT_TEMPLATES 注册了 cinematographer-planner 和 cinematographer-executor
- cinematographer.ts Node 支持 useMultiStage 选项
- PromptStudio 可以分别配置两个阶段
- 默认保持单阶段行为（向后兼容）
- Node 代码简化，视频生成逻辑在 Agent 内部
- 使用中文日志输出
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent/04-03-SUMMARY.md`
</output>
